// stack( primitive datatype ) copy of original values which are predefined in js
let myYoutubename = "samratneupane.com.bp";

let mychanem = myYoutubename;
mychanem = "sdf";
console.log(myYoutubename);

//in stack any changes made in stack , is changed in copied values

// heap(non primitive datatype)  we get reefernce to orginal vales are the reference values in js

//in heap if any changes done than it is done in original values
